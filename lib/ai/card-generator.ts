// Minimal stub to satisfy analyze flow without cross-project deps.
export async function saveUsedCardName(_cardId: string, _name: string) {
  // No-op; extend to persist used names if needed.
  return;
}
